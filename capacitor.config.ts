import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.etienne.marketplace',
  appName: 'Marketplace',
  webDir: 'dist/marketplace-frontend/browser'
};

export default config;
